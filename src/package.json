{
  "name": "electron-template",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager ./ template --platform=win32 --arch=x64 --overwrite --out=build",
    "run": "npm run build && \"./build/template-win32-x64/template.exe\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^35.0.1",
    "electron-packager": "^17.1.2"
  }
}
